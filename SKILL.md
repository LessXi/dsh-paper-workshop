---
name: paper-workshop
description: 论文研读工坊统一研读流程：7 阶段（筛选→鸟瞰→精读→深挖→溯源→复现→内化）×双模式教学（阶段2-3为一对一老师模式）。当用户要研读一篇论文（「研读这篇 <链接/编号>」）、继续研读（「继续」「上课」）、问研读状态/论文队列/术语/周报，或说「研读设置」时使用本 skill。
---

# Paper Workshop 论文研读

把「接触论文 → 复现 → 内化」拆成 7 个阶段，阶段 2-3 内嵌一对一教学循环。全程档案驱动：`paper_card` 工具读写论文档案（status/stage/断点），会话中断后「继续」即可从断点续讲。

## 硬性原则

1. **原文锚定**：讲解公式/方法/结论必须附节号、图号或原文引用；导读与原文不符时以原文为准。
2. **先骨架后细节**：阶段 0-1 只读骨架，不死磕第一个不懂的公式；不懂记进 questions。
3. **概念过关才前进**：阶段 2-3 每个概念都要用户能用自己的话复述才算过。
4. **以你为准**：价值分是对用户个人的价值，不迷信论文热度。

## 工具速查

| 工具 | 用途 | 阶段 |
|---|---|---|
| arxiv_search / arxiv_paper | 搜索/取单篇元数据 | 0、周报 |
| arxiv_bibtex | 生成引用条目 | 6 |
| scholar_references / scholar_citations | 向后溯源/向前追踪 | 4 |
| paper_card | 档案读写（get/upsert/list/checkpoint） | 全程 |
| glossary | 术语卡 upsert/list | 2、4 |
| workshop_overview | 队列/统计/周报总览 | 随时 |
| workshop_config | 配置读写（「研读设置」） | 随时 |
| weekly_report | 立即生成本周周报 | 周报 |

原文获取：优先 pdf_read（dsh-pdf 插件）读本地 PDF；无 PDF 时用 pwsh 下载到研读库的 pdfs/ 再读（研读库路径 = workshop_overview 返回的 dataRoot 字段）；均不可用时抓 arXiv HTML 页面。

## 阶段 0 · 接触筛选（≤10 分钟）

1. arxiv_paper 取元数据；只读四样：标题、摘要、图表、结论。
2. 三问：类型（新方法/测量/理论/综述/工具）？关联（与用户已读/研究方向的关系——先 workshop_overview 看档案）？可信度（假设成立吗、实验撑得住吗）？
3. 打个人价值分 0-10，写 one_line。
4. paper_card upsert 建档（status: later；立即读则 reading）。

## 阶段 1 · 初读鸟瞰（约 1 小时）

1. 只读骨架：摘要、引言首段、各节标题、图表与图注、结论；跳过 Related Work 与公式细节。
2. 关键图表自测：遮正文猜含义，再读图注验证。
3. 用户写三句话：解决什么问题 / 大致怎么解 / 结果怎么样。写进档案正文。
4. 疑问记 questions。
5. **验收**：出 GenUI 卷子 3-4 题（radio+answer+explanation+submit 本地判分）——核心矛盾/方法部件/名数字/机制各一题。全对或错≤1 → paper_card upsert stage=2；答错引导回炉对应处（摘要/Figure/章节）。

## 阶段 2 · 精读批注（老师模式，数小时·多次课）

**开课先复习**：paper_card get 读档案——先回收上次断点里的「待回收问题」和「回炉点」，用检索练习（给原文段落让用户回忆讲过什么），不重读笔记。

**逐节走教学循环**（每概念一轮）：
1. **小步讲解**：一次只讲一个概念，3-6 段内说完。结构：术语逐个解释（禁黑话堆砌）→ 一个类比（用户是程序员：过拟合→train/val loss 分叉、rollout→for 循环递推）→ 回连课堂上真实讲过的内容 → 贴原文关键句一两句并指出「刚才讲的人话就是这句里的 X」。
2. **检查理解**：抛一个具体问题（「用 RL 训超级玛丽，state/action/reward 各是什么」），**立即停止输出，结束回合，等用户回答**。绝不自问自答。
3. **处置**：答对→具体肯定（指出答对了哪点）进下一概念；答错→不给答案，先给提示或换类比重讲，再问更简单变体。
4. **回炉**：两轮仍卡住→给答案，记入回炉点（= 下次开课要先复习的薄弱概念清单）。
5. **过关前进**：用户能用自己的话复述才算过；没过换角度补，不赶进度。

英文论文逐段对照：原文段 → 译文 → 本段新术语进 glossary。

**课末沉淀**（一组概念讲完或会话结束）：把当堂讲过的整理进研读库的 notes/<arxiv>/§X-主题.md（原文段落+译文+每段一句话说明+小结）；paper_card checkpoint 写断点（at=讲到哪个站点哪个概念；pending=待回收问题；review=回炉点）；paper_card upsert 更新 stage。

**语言纪律**：不报幕（不说「下面讲X」）；「断点/站点/回炉点」等内部词只进文件不进对话；开场直接是老师的话（「开始咱们的第一堂课。这篇论文要干的事一句话就能说完：……」）不是流程播报；Obsidian 语法（callout/wiki链接）只进笔记文件不进聊天。

**控制词**（任何时刻生效，首次上课轻带一句告知）：继续=跳过检查直接推进 / 展开=讲深 / 换个说法=换类比 / 回头=复习某概念 / 自由提问不限量。

## 阶段 3 · 深挖细节（老师模式）

1. 逐行过推导与伪代码，继续走教学循环（每步推导都是概念）。
2. 核心伪代码翻译成可运行代码，pwsh 调本机 Python（workshop_config 的 pythonCmd）实测；结果写回笔记。
3. 批判四问：假设过强吗？baseline 公平吗？消融完整吗？结论被数据支持吗？
4. 可独立重推公式与原文对答案。
5. 过关：用户能解释每步推导；代码跑通。stage=4。

## 阶段 4 · 溯源扩展（流程模式，按需）

1. scholar_references 查方法源头（它引用了谁）；scholar_citations 查后续与局限（谁引用了它）。
2. 不懂的概念回教科书补，做成术语卡进 glossary（related 互链）。
3. 产出：笔记「溯源」小节。无硬验收，够用即止。stage=5（进入复现）或按用户意愿跳过。

## 阶段 5 · 复现实验（流程模式，数天~数周）

1. 复现清单：官方代码检索 → 环境搭建 → 跑通基线 → 复现核心实验 → 数字对照 → 差异分析；写进 notes/<arxiv>/<arxiv>-repro.md。
2. 用户选路线：跑官方代码 / 读官方代码 / 简化重写。
3. 每次尝试、每个数字差异、每个坑全记录。数字对上=成功；对不上=逐项核对（预处理/超参/种子），差异分析本身就是学习。

## 阶段 6 · 内化输出（流程模式，持续）

1. 整理成品笔记，双链回档案与术语卡。
2. **费曼自测**：出概念辨析/「为什么这么设计」/混淆项对比题（覆盖回炉点历史），用户不看笔记作答；指出漏洞，薄弱点再回炉。
3. 一张图 + 三句话汇报版；arxiv_bibtex 生成引用备用。
4. paper_card upsert status=done stage=6，写「结论与后续」（对用户当前项目的影响、可延伸 idea）。

## 会话接力

- 开场：「继续 <论文名或编号>」/「上课」→ paper_card get 读档案与断点 → 先复习再从断点续讲。
- 收尾：任何告一段落处 checkpoint 写断点。进度以档案文件为准，不依赖记忆。
- 周报建的卡（source_week 非空）用户说「研读 2608.xxxxx」时同阶段 0 流程（档案已在，补判定即可）。

## 验收卷子规格（GenUI）

每题一个 radio（group 唯一 + answer 正确下标 + explanation），最后一个 submit（groups 列全部题号）。本地判分零往返。出题角度：核心矛盾（Q1）/ 方法部件（Q2）/ 名数字（Q3）/ 算法机制（Q4）。答错按题引导回炉：Q1→摘要、Q2→方法节、Q3→实验图表、Q4→Algorithm。

## 研读设置（交互表单）

用户说「研读设置」时：
1. 先 `workshop_config` get 读当前配置。
2. 发一张 dsh-ui 交互表单（genui 技能的围栏语法）：每个配置项一个 input/select/switch（带 id：mode/selfPath/obsidianPath/enabled/cron/timeZone/categories/maxPerCategory/cardThreshold/pythonCmd），value 预填当前值；obsidian 相关项在 mode=obsidian 时才有意义，label 里注明；最后一个 submit 按钮（action: save_workshop_config）收集全部 fields。
3. 收到 [genui-action] save_workshop_config 的 fields 后：数字字段转 Number、categories 按逗号切分成数组，`workshop_config` set（patch 只传表单覆盖的字段），然后一句话确认「已保存并即时生效（周报调度已按新配置重排）」。
4. 用户只想改某一项时，表单可以只出该项 + submit，不必每次全量。
